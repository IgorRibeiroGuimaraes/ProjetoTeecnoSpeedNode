generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model CartaVAN {
  id        String   @id @default(uuid())
  usuarioId String
  conteudo  String
  criadoEm  DateTime @default(now())
}
